import { Meta } from "@storybook/blocks";

<Meta title="Tokens/Colors/Theme Classes" />

export const colors = {
  surface: [
    { 
      name: '--surface-bright', 
      value: 'var(--surface-bright)', 
      global: 'var(--global-white)',
      classes: {
        text: 'surface-bright',
        background: 'surface-bright-bg',
        border: 'surface-bright-border'
      }
    },
    { 
      name: '--surface', 
      value: 'var(--surface)', 
      global: 'var(--global-neutral-20)',
      classes: {
        text: 'surface',
        background: 'surface-bg',
        border: 'surface-border'
      }
    },
    { 
      name: '--surface-dim', 
      value: 'var(--surface-dim)', 
      global: 'var(--global-neutral-40)',
      classes: {
        text: 'surface-dim',
        background: 'surface-dim-bg',
        border: 'surface-dim-border'
      }
    }
  ],
  surfaceContainer: [
    { 
      name: '--surface-container-lowest', 
      value: 'var(--surface-container-lowest)', 
      global: 'var(--global-white)',
      classes: {
        text: 'surface-container-lowest',
        background: 'surface-container-lowest-bg',
        border: 'surface-container-lowest-border'
      }
    },
    { 
      name: '--surface-container-low', 
      value: 'var(--surface-container-low)', 
      global: 'var(--global-neutral-10)',
      classes: {
        text: 'surface-container-low',
        background: 'surface-container-low-bg',
        border: 'surface-container-low-border'
      }
    },
    { 
      name: '--surface-container', 
      value: 'var(--surface-container)', 
      global: 'var(--global-neutral-30)',
      classes: {
        text: 'surface-container',
        background: 'surface-container-bg',
        border: 'surface-container-border'
      }
    },
    { 
      name: '--surface-container-high', 
      value: 'var(--surface-container-high)', 
      global: 'var(--global-neutral-40)',
      classes: {
        text: 'surface-container-high',
        background: 'surface-container-high-bg',
        border: 'surface-container-high-border'
      }
    },
    { 
      name: '--surface-container-highest', 
      value: 'var(--surface-container-highest)', 
      global: 'var(--global-neutral-100)',
      classes: {
        text: 'surface-container-highest',
        background: 'surface-container-highest-bg',
        border: 'surface-container-highest-border'
      }
    }
  ],
  onSurface: [
    { 
      name: '--on-surface-lowest', 
      value: 'var(--on-surface-lowest)', 
      global: 'var(--global-neutral-300)',
      classes: {
        text: 'on-surface-lowest',
        background: 'on-surface-lowest-bg',
        border: 'on-surface-lowest-border'
      }
    },
    { 
      name: '--on-surface-low', 
      value: 'var(--on-surface-low)', 
      global: 'var(--global-neutral-500)',
      classes: {
        text: 'on-surface-low',
        background: 'on-surface-low-bg',
        border: 'on-surface-low-border'
      }
    },
    { 
      name: '--on-surface', 
      value: 'var(--on-surface)', 
      global: 'var(--global-neutral-700)',
      classes: {
        text: 'on-surface',
        background: 'on-surface-bg',
        border: 'on-surface-border'
      }
    }
  ],
  outline: [
    { 
      name: '--outline-bright', 
      value: 'var(--outline-bright)', 
      global: 'var(--global-neutral-30)',
      classes: {
        text: 'outline-bright',
        background: 'outline-bright-bg',
        border: 'outline-bright-border'
      }
    },
    { 
      name: '--outline', 
      value: 'var(--outline)', 
      global: 'var(--global-neutral-100)',
      classes: {
        text: 'outline',
        background: 'outline-bg',
        border: 'outline-border'
      }
    },
    { 
      name: '--outline-dim', 
      value: 'var(--outline-dim)', 
      global: 'var(--global-neutral-300)',
      classes: {
        text: 'outline-dim',
        background: 'outline-dim-bg',
        border: 'outline-dim-border'
      }
    }
  ],
  primary: [
    { 
      name: '--primary', 
      value: 'var(--primary)', 
      global: 'var(--global-charcoal-blue-700)',
      classes: {
        text: 'primary',
        background: 'primary-bg',
        border: 'primary-border'
      }
    },
    { 
      name: '--on-primary', 
      value: 'var(--on-primary)', 
      global: 'var(--global-white)',
      classes: {
        text: 'on-primary',
        background: 'on-primary-bg',
        border: 'on-primary-border'
      }
    },
    { 
      name: '--primary-container', 
      value: 'var(--primary-container)', 
      global: 'var(--global-charcoal-blue-50)',
      classes: {
        text: 'primary-container',
        background: 'primary-container-bg',
        border: 'primary-container-border'
      }
    },
    { 
      name: '--on-primary-container', 
      value: 'var(--on-primary-container)', 
      global: 'var(--global-charcoal-blue-700)',
      classes: {
        text: 'on-primary-container',
        background: 'on-primary-container-bg',
        border: 'on-primary-container-border'
      }
    }
  ],
  secondary: [
    { 
      name: '--secondary', 
      value: 'var(--secondary)', 
      global: 'var(--global-aqua-green-600)',
      classes: {
        text: 'secondary',
        background: 'secondary-bg',
        border: 'secondary-border'
      }
    },
    { 
      name: '--on-secondary', 
      value: 'var(--on-secondary)', 
      global: 'var(--global-white)',
      classes: {
        text: 'on-secondary',
        background: 'on-secondary-bg',
        border: 'on-secondary-border'
      }
    },
    { 
      name: '--secondary-container', 
      value: 'var(--secondary-container)', 
      global: 'var(--global-aqua-green-50)',
      classes: {
        text: 'secondary-container',
        background: 'secondary-container-bg',
        border: 'secondary-container-border'
      }
    },
    { 
      name: '--on-secondary-container', 
      value: 'var(--on-secondary-container)', 
      global: 'var(--global-aqua-green-600)',
      classes: {
        text: 'on-secondary-container',
        background: 'on-secondary-container-bg',
        border: 'on-secondary-container-border'
      }
    }
  ],
  status: {
    error: [
      { 
        name: '--error', 
        value: 'var(--error)', 
        global: 'var(--global-red-600)',
        classes: {
          text: 'error',
          background: 'error-bg',
          border: 'error-border'
        }
      },
      { 
        name: '--on-error', 
        value: 'var(--on-error)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-error',
          background: 'on-error-bg',
          border: 'on-error-border'
        }
      },
      { 
        name: '--error-container', 
        value: 'var(--error-container)', 
        global: 'var(--global-red-50)',
        classes: {
          text: 'error-container',
          background: 'error-container-bg',
          border: 'error-container-border'
        }
      },
      { 
        name: '--on-error-container', 
        value: 'var(--on-error-container)', 
        global: 'var(--global-red-600)',
        classes: {
          text: 'on-error-container',
          background: 'on-error-container-bg',
          border: 'on-error-container-border'
        }
      }
    ],
    warning: [
      { 
        name: '--warning', 
        value: 'var(--warning)', 
        global: 'var(--global-yellow-600)',
        classes: {
          text: 'warning',
          background: 'warning-bg',
          border: 'warning-border'
        }
      },
      { 
        name: '--on-warning', 
        value: 'var(--on-warning)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-warning',
          background: 'on-warning-bg',
          border: 'on-warning-border'
        }
      },
      { 
        name: '--warning-container', 
        value: 'var(--warning-container)', 
        global: 'var(--global-yellow-50)',
        classes: {
          text: 'warning-container',
          background: 'warning-container-bg',
          border: 'warning-container-border'
        }
      },
      { 
        name: '--on-warning-container', 
        value: 'var(--on-warning-container)', 
        global: 'var(--global-yellow-600)',
        classes: {
          text: 'on-warning-container',
          background: 'on-warning-container-bg',
          border: 'on-warning-container-border'
        }
      }
    ],
    success: [
      { 
        name: '--success', 
        value: 'var(--success)', 
        global: 'var(--global-green-600)',
        classes: {
          text: 'success',
          background: 'success-bg',
          border: 'success-border'
        }
      },
      { 
        name: '--on-success', 
        value: 'var(--on-success)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-success',
          background: 'on-success-bg',
          border: 'on-success-border'
        }
      },
      { 
        name: '--success-container', 
        value: 'var(--success-container)', 
        global: 'var(--global-green-50)',
        classes: {
          text: 'success-container',
          background: 'success-container-bg',
          border: 'success-container-border'
        }
      },
      { 
        name: '--on-success-container', 
        value: 'var(--on-success-container)', 
        global: 'var(--global-green-600)',
        classes: {
          text: 'on-success-container',
          background: 'on-success-container-bg',
          border: 'on-success-container-border'
        }
      }
    ]
  },
  extended: {
    extend1: [
      { 
        name: '--extend-1', 
        value: 'var(--extend-1)', 
        global: 'var(--global-pink-600)',
        classes: {
          text: 'extend-1',
          background: 'extend-1-bg',
          border: 'extend-1-border'
        }
      },
      { 
        name: '--on-extend-1', 
        value: 'var(--on-extend-1)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-extend-1',
          background: 'on-extend-1-bg',
          border: 'on-extend-1-border'
        }
      },
      { 
        name: '--extend-1-container', 
        value: 'var(--extend-1-container)', 
        global: 'var(--global-pink-50)',
        classes: {
          text: 'extend-1-container',
          background: 'extend-1-container-bg',
          border: 'extend-1-container-border'
        }
      },
      { 
        name: '--on-extend-1-container', 
        value: 'var(--on-extend-1-container)', 
        global: 'var(--global-pink-600)',
        classes: {
          text: 'on-extend-1-container',
          background: 'on-extend-1-container-bg',
          border: 'on-extend-1-container-border'
        }
      }
    ],
    extend2: [
      { 
        name: '--extend-2', 
        value: 'var(--extend-2)', 
        global: 'var(--global-purple-700)',
        classes: {
          text: 'extend-2',
          background: 'extend-2-bg',
          border: 'extend-2-border'
        }
      },
      { 
        name: '--on-extend-2', 
        value: 'var(--on-extend-2)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-extend-2',
          background: 'on-extend-2-bg',
          border: 'on-extend-2-border'
        }
      },
      { 
        name: '--extend-2-container', 
        value: 'var(--extend-2-container)', 
        global: 'var(--global-purple-50)',
        classes: {
          text: 'extend-2-container',
          background: 'extend-2-container-bg',
          border: 'extend-2-container-border'
        }
      },
      { 
        name: '--on-extend-2-container', 
        value: 'var(--on-extend-2-container)', 
        global: 'var(--global-purple-700)',
        classes: {
          text: 'on-extend-2-container',
          background: 'on-extend-2-container-bg',
          border: 'on-extend-2-container-border'
        }
      }
    ],
    extend3: [
      { 
        name: '--extend-3', 
        value: 'var(--extend-3)', 
        global: 'var(--global-sky-blue-600)',
        classes: {
          text: 'extend-3',
          background: 'extend-3-bg',
          border: 'extend-3-border'
        }
      },
      { 
        name: '--on-extend-3', 
        value: 'var(--on-extend-3)', 
        global: 'var(--global-white)',
        classes: {
          text: 'on-extend-3',
          background: 'on-extend-3-bg',
          border: 'on-extend-3-border'
        }
      },
      { 
        name: '--extend-3-container', 
        value: 'var(--extend-3-container)', 
        global: 'var(--global-sky-blue-50)',
        classes: {
          text: 'extend-3-container',
          background: 'extend-3-container-bg',
          border: 'extend-3-container-border'
        }
      },
      { 
        name: '--on-extend-3-container', 
        value: 'var(--on-extend-3-container)', 
        global: 'var(--global-sky-blue-600)',
        classes: {
          text: 'on-extend-3-container',
          background: 'on-extend-3-container-bg',
          border: 'on-extend-3-container-border'
        }
      }
    ]
  }
};

<div className="sb-container">
  <div className="sb-section-title">
    # Colors (Theme Classes)
    These tokens should be used for consistent color styling across applications.
  </div>

  <div className="sb-section">
    <div className="sb-colors-demo">
      <h2>Surface Colors</h2>
      <div className="color-grid">
        {colors.surface.map(({ value, name, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Surface Container Colors</h2>
      <div className="color-grid">
        {colors.surfaceContainer.map(({ name, value, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>On Surface Colors</h2>
      <div className="color-grid">
        {colors.onSurface.map(({ name, value, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Outline Colors</h2>
      <div className="color-grid">
        {colors.outline.map(({ name, value, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Primary Colors</h2>
      <div className="color-grid">
        {colors.primary.map(({ name, value, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Secondary Colors</h2>
      <div className="color-grid">
        {colors.secondary.map(({ name, value, global, classes }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Status Colors</h2>
      {Object.entries(colors.status).map(([statusType, statusColors]) => (
        <div key={statusType} className="color-section">
          <h3>{statusType.charAt(0).toUpperCase() + statusType.slice(1)}</h3>
          <div className="color-grid">
            {statusColors.map(({ name, value, global, classes }, index) => (
              <div key={index} className="color-item">
                <div className="color-preview" style={{ backgroundColor: value }}></div>
                <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}

      <h2>Extended Colors</h2>
      {Object.entries(colors.extended).map(([extendType, extendColors]) => (
        <div key={extendType} className="color-section">
          <h3>{extendType.replace(/([A-Z])/g, ' $1').trim()}</h3>
          <div className="color-grid">
            {extendColors.map(({ name, value, global, classes }, index) => (
              <div key={index} className="color-item">
                <div className="color-preview" style={{ backgroundColor: value }}></div>
                <div className="color-info">
                  <div className="color-classes">
                    <h4>Classes</h4>
                    <code>Text: {classes.text}</code>
                    <code>Background: {classes.background}</code>
                    <code>Border: {classes.border}</code>
                  </div>
                  <div className="color-variables">
                    <h4>Variable</h4>
                    <code>{name}</code>
                  </div>
                  <span>{global}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  {`
    .sb-container {
      margin-bottom: 48px;
    }

    .sb-section-title {
      margin-bottom: 32px;
    }

    .sb-colors-demo {
      width: 100%;
    }

    h2 {
      margin: 32px 0 16px;
      padding-bottom: 8px;
      border-bottom: 1px solid #E6E6E6;
    }

    h3 {
      margin-top: 24px;
      margin-bottom: 16px;
    }

    .color-section {
      margin-bottom: 32px;
    }

    .color-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }

    .color-item {
      border: 1px solid #E6E6E6;
      border-radius: 4px;
      overflow: hidden;
    }

    .color-preview {
      height: 100px;
      border-bottom: 1px solid #E6E6E6;
    }

    .color-info {
      padding: 12px;
      background: #FFFFFF;
    }

    .color-info code {
      margin-bottom: 4px;
      font-family: monospace;
      font-size: 12px;
      color: #666;
    }

    .color-info span {
      font-size: 14px;
      color: #333;
    }

    .color-item:hover {
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .color-example {
      font-size: 12px;
      color: inherit;
      text-align: center;
      opacity: 0.7;
    }

    .color-classes {
      overflow-y: scroll;
    }

    .color-variables h4 {
      margin-top: 10px !important;
    }
  `}
</style> 