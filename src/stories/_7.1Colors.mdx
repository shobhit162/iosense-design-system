import { Meta } from "@storybook/blocks";

<Meta title="Tokens/Colors/Theme" />

export const colors = {
  surface: [
    { name: '--surface-bright', value: 'var(--surface-bright)', global: 'var(--global-white)' },
    { name: '--surface', value: 'var(--surface)', global: 'var(--global-neutral-20)' },
    { name: '--surface-dim', value: 'var(--surface-dim)', global: 'var(--global-neutral-40)' }
  ],
  surfaceContainer: [
    { name: '--surface-container-lowest', value: 'var(--surface-container-lowest)', global: 'var(--global-white)' },
    { name: '--surface-container-low', value: 'var(--surface-container-low)', global: 'var(--global-neutral-10)' },
    { name: '--surface-container', value: 'var(--surface-container)', global: 'var(--global-neutral-30)' },
    { name: '--surface-container-high', value: 'var(--surface-container-high)', global: 'var(--global-neutral-40)' },
    { name: '--surface-container-highest', value: 'var(--surface-container-highest)', global: 'var(--global-neutral-100)' }
  ],
  onSurface: [
    { name: '--on-surface-lowest', value: 'var(--on-surface-lowest)', global: 'var(--global-neutral-300)' },
    { name: '--on-surface-low', value: 'var(--on-surface-low)', global: 'var(--global-neutral-500)' },
    { name: '--on-surface', value: 'var(--on-surface)', global: 'var(--global-neutral-700)' }
  ],
  outline: [
    { name: '--outline-bright', value: 'var(--outline-bright)', global: 'var(--global-neutral-30)' },
    { name: '--outline', value: 'var(--outline)', global: 'var(--global-neutral-100)' },
    { name: '--outline-dim', value: 'var(--outline-dim)', global: 'var(--global-neutral-300)' }
  ],
  primary: [
    { name: '--primary', value: 'var(--primary)', global: 'var(--global-charcoal-blue-700)' },
    { name: '--on-primary', value: 'var(--on-primary)', global: 'var(--global-white)' },
    { name: '--primary-container', value: 'var(--primary-container)', global: 'var(--global-charcoal-blue-50)' },
    { name: '--on-primary-container', value: 'var(--on-primary-container)', global: 'var(--global-charcoal-blue-700)' }
  ],
  secondary: [
    { name: '--secondary', value: 'var(--secondary)', global: 'var(--global-aqua-green-600)' },
    { name: '--on-secondary', value: 'var(--on-secondary)', global: 'var(--global-white)' },
    { name: '--secondary-container', value: 'var(--secondary-container)', global: 'var(--global-aqua-green-50)' },
    { name: '--on-secondary-container', value: 'var(--on-secondary-container)', global: 'var(--global-aqua-green-600)' }
  ],
  status: {
    error: [
      { name: '--error', value: 'var(--error)', global: 'var(--global-red-600)' },
      { name: '--on-error', value: 'var(--on-error)', global: 'var(--global-white)' },
      { name: '--error-container', value: 'var(--error-container)', global: 'var(--global-red-50)' },
      { name: '--on-error-container', value: 'var(--on-error-container)', global: 'var(--global-red-600)' }
    ],
    warning: [
      { name: '--warning', value: 'var(--warning)', global: 'var(--global-yellow-600)' },
      { name: '--on-warning', value: 'var(--on-warning)', global: 'var(--global-white)' },
      { name: '--warning-container', value: 'var(--warning-container)', global: 'var(--global-yellow-50)' },
      { name: '--on-warning-container', value: 'var(--on-warning-container)', global: 'var(--global-yellow-600)' }
    ],
    success: [
      { name: '--success', value: 'var(--success)', global: 'var(--global-green-600)' },
      { name: '--on-success', value: 'var(--on-success)', global: 'var(--global-white)' },
      { name: '--success-container', value: 'var(--success-container)', global: 'var(--global-green-50)' },
      { name: '--on-success-container', value: 'var(--on-success-container)', global: 'var(--global-green-600)' }
    ]
  },
  extended: {
    extend1: [
      { name: '--extend-1', value: 'var(--extend-1)', global: 'var(--global-pink-600)' },
      { name: '--on-extend-1', value: 'var(--on-extend-1)', global: 'var(--global-white)' },
      { name: '--extend-1-container', value: 'var(--extend-1-container)', global: 'var(--global-pink-50)' },
      { name: '--on-extend-1-container', value: 'var(--on-extend-1-container)', global: 'var(--global-pink-600)' }
    ],
    extend2: [
      { name: '--extend-2', value: 'var(--extend-2)', global: 'var(--global-purple-700)' },
      { name: '--on-extend-2', value: 'var(--on-extend-2)', global: 'var(--global-white)' },
      { name: '--extend-2-container', value: 'var(--extend-2-container)', global: 'var(--global-purple-50)' },
      { name: '--on-extend-2-container', value: 'var(--on-extend-2-container)', global: 'var(--global-purple-700)' }
    ],
    extend3: [
      { name: '--extend-3', value: 'var(--extend-3)', global: 'var(--global-sky-blue-600)' },
      { name: '--on-extend-3', value: 'var(--on-extend-3)', global: 'var(--global-white)' },
      { name: '--extend-3-container', value: 'var(--extend-3-container)', global: 'var(--global-sky-blue-50)' },
      { name: '--on-extend-3-container', value: 'var(--on-extend-3-container)', global: 'var(--global-sky-blue-600)' }
    ]
  }
};

<div className="sb-container">
  <div className="sb-section-title">
    # Colors (Theme Variables)
    These tokens should be used for consistent color styling across applications.
  </div>

  <div className="sb-section">
    <div className="sb-colors-demo">
      <h2>Surface Colors</h2>
      <div className="color-grid">
        {colors.surface.map(({ value, name, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Surface Container Colors</h2>
      <div className="color-grid">
        {colors.surfaceContainer.map(({ name, value, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>On Surface Colors</h2>
      <div className="color-grid">
        {colors.onSurface.map(({ name, value, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Outline Colors</h2>
      <div className="color-grid">
        {colors.outline.map(({ name, value, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Primary Colors</h2>
      <div className="color-grid">
        {colors.primary.map(({ name, value, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Secondary Colors</h2>
      <div className="color-grid">
        {colors.secondary.map(({ name, value, global }, index) => (
          <div key={index} className="color-item">
            <div className="color-preview" style={{ backgroundColor: value }}></div>
            <div className="color-info">
              <code>{name}</code>
              <span>{global}</span>
            </div>
          </div>
        ))}
      </div>

      <h2>Status Colors</h2>
      {Object.entries(colors.status).map(([statusType, statusColors]) => (
        <div key={statusType} className="color-section">
          <h3>{statusType.charAt(0).toUpperCase() + statusType.slice(1)}</h3>
          <div className="color-grid">
            {statusColors.map(({ name, value, global }, index) => (
              <div key={index} className="color-item">
                <div className="color-preview" style={{ backgroundColor: value }}></div>
                <div className="color-info">
                  <code>{name}</code>
                  <span>{global}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}

      <h2>Extended Colors</h2>
      {Object.entries(colors.extended).map(([extendType, extendColors]) => (
        <div key={extendType} className="color-section">
          <h3>{extendType.replace(/([A-Z])/g, ' $1').trim()}</h3>
          <div className="color-grid">
            {extendColors.map(({ name, value, global }, index) => (
              <div key={index} className="color-item">
                <div className="color-preview" style={{ backgroundColor: value }}></div>
                <div className="color-info">
                  <code>{name}</code>
                  <span>{global}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  {`
    .sb-container {
      margin-bottom: 48px;
    }

    .sb-section-title {
      margin-bottom: 32px;
    }

    .sb-colors-demo {
      width: 100%;
    }

    h2 {
      margin: 32px 0 16px;
      padding-bottom: 8px;
      border-bottom: 1px solid #E6E6E6;
    }

    h3 {
      margin-top: 24px;
      margin-bottom: 16px;
    }

    .color-section {
      margin-bottom: 32px;
    }

    .color-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }

    .color-item {
      border: 1px solid #E6E6E6;
      border-radius: 4px;
      overflow: hidden;
    }

    .color-preview {
      height: 100px;
      border-bottom: 1px solid #E6E6E6;
    }

    .color-info {
      padding: 12px;
      background: #FFFFFF;
    }

    .color-info code {
      display: block;
      margin-bottom: 4px;
      font-family: monospace;
      font-size: 12px;
      color: #666;
    }

    .color-info span {
      font-size: 14px;
      color: #333;
    }

    .color-item:hover {
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .color-example {
      font-size: 12px;
      color: inherit;
      text-align: center;
      opacity: 0.7;
    }
  `}
</style> 