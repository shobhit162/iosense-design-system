import { Meta } from "@storybook/blocks";

<Meta title="Tokens/Spacing/Margin" name="_6Spacing"/>

export const spacing = {
  values: [  
    { key: '00', name: 'spacing-00', value: '0px' },
    { key: '01', name: 'spacing-01', value: '2px' },
    { key: '02', name: 'spacing-02', value: '4px' },
    { key: '03', name: 'spacing-03', value: '8px' },
    { key: '04', name: 'spacing-04', value: '12px' },
    { key: '05', name: 'spacing-05', value: '16px' },
    { key: '06', name: 'spacing-06', value: '20px' },
    { key: '07', name: 'spacing-07', value: '24px' },
    { key: '08', name: 'spacing-08', value: '32px' },
    { key: '09', name: 'spacing-09', value: '40px' },
    { key: '10', name: 'spacing-10', value: '48px' },
    { key: '11', name: 'spacing-11', value: '56px' }
  ]
};

<div className="sb-container">
  <div className="sb-section-title">
    # Spacing
  </div>

  <div className="sb-section">

<h2>Margin Utilities</h2>
<div className="sb-demo-section">
  <div className="sb-utility-group">
    <h3>All Sides Margin</h3>
    <div className="sb-demo-box">
      {spacing.values.map((item) => (
        <div key={item.key} className="sb-demo-content" style={{ 
          margin: item.value, 
          background: '#E5E7EB',
        }}>
          <code>.global-m-{item.key}</code>
          <p>Margin: {item.name} ({item.value})</p>
        </div>
      ))}
    </div>
  </div>

  <div className="sb-utility-group">
    <h3>Horizontal Margin (mx)</h3>
    <div className="sb-demo-box">
      {spacing.values.map((item) => (
        <div key={item.key} className="sb-demo-content" style={{ 
          marginLeft: item.value, 
          marginRight: item.value,
          background: '#E5E7EB',
        }}>
          <code>.global-mx-{item.key}</code>
          <p>Horizontal Margin: {item.name} ({item.value})</p>
        </div>
      ))}
    </div>
  </div>

  <div className="sb-utility-group">
    <h3>Vertical Margin (my)</h3>
    <div className="sb-demo-box">
      {spacing.values.map((item) => (
        <div key={item.key} className="sb-demo-content" style={{ 
          marginTop: item.value, 
          marginBottom: item.value,
          background: '#E5E7EB',
        }}>
          <code>.global-my-{item.key}</code>
          <p>Vertical Margin: {item.name} ({item.value})</p>
        </div>
      ))}
    </div>
  </div>

  <div className="sb-utility-group">
    <h3>Individual Margin Sides</h3>
    <div className="sb-demo-box">
      {spacing.values.map((item) => (
        <div key={item.key} className="sb-demo-content">
          <div style={{ marginTop: item.value, background: '#E5E7EB'}}>
            <code>.global-mt-{item.key}</code>
            <p>Top Margin: {item.name} ({item.value})</p>
          </div>
          <div style={{ marginRight: item.value, background: '#E5E7EB'}}>
            <code>.global-mr-{item.key}</code>
            <p>Right Margin: {item.name} ({item.value})</p>
          </div>
          <div style={{ marginBottom: item.value, background: '#E5E7EB'}}>
            <code>.global-mb-{item.key}</code>
            <p>Bottom Margin: {item.name} ({item.value})</p>
          </div>
          <div style={{ marginLeft: item.value, background: '#E5E7EB'}}>
            <code>.global-ml-{item.key}</code>
            <p>Left Margin: {item.name} ({item.value})</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>




    <h2>Usage Examples</h2>
    <div className="sb-code-block">
      ```html
      <!-- Margin Examples -->
      <div class="global-m-05">All sides margin 16px</div>
      <div class="global-mx-05">Horizontal margin 16px</div>
      <div class="global-mt-05">Top margin 16px</div>
      ```
    </div>
  </div>
</div>

<style>
  {`
    .sb-container {
      margin-bottom: 48px;
    }

    .sb-section-title {
      margin-bottom: 32px;
    }

    .sb-spacing-scale {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 24px;
      margin-bottom: 48px;
    }

    .sb-scale-item {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .sb-scale-visual {
      width: 56px;
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .sb-scale-box {
      min-width: 2px;
      min-height: 2px;
    }

    .sb-scale-info {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .sb-scale-info code {
      font-family: monospace;
      font-size: 14px;
      color: #666;
    }

    .sb-scale-info span {
      font-size: 12px;
      color: #888;
    }

    .sb-demo-section {
      display: grid;
      gap: 32px;
      margin-bottom: 48px;
    }

    .sb-utility-group {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .sb-demo-box {
      padding: 24px;
      background: #F9FAFB;
      border-radius: 8px;
      border: 1px solid #E5E7EB;
    }

    .sb-demo-content {
      padding: 8px;
      border-radius: 4px;
    }

    .sb-code-block {
      background: #F9FAFB;
      padding: 16px;
      border-radius: 8px;
      margin-top: 16px;
      overflow: auto;
    }

    code {
      font-family: monospace;
      font-size: 14px;
      padding: 2px 4px;
      background: #F3F4F6;
      border-radius: 4px;
    }

    h2 {
      margin: 32px 0 16px;
      padding-bottom: 8px;
      border-bottom: 1px solid #E6E6E6;
    }

    h3 {
      margin: 0;
      font-size: 16px;
      color: #374151;
    }
  `}
</style>